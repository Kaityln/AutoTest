<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE suite SYSTEM "http://testng.org/testng-1.0.dtd" >

<suite name="thread" parallel="classes" thread-count="3">
    <!-- parallel属性: 指定执行用例级别

    *tests(指的是当前文件中的标签)：不同的test tag下的用例可以在不同的线程下执行
           相同test tag 下的测试用例只能在相同的线程下执行

    *classes(指的是当前文件中的标签):需要给classes 命名，即：配置name属性
             相同的class tag下的用例可以在同一线程中执行
             不同的class tag 下的用例可以在不同的线程中执行

    *methods(指的是当前文件中的标签): 表示方法级别，所有用例都可以在不同的线程下执行

    ===================================
    thread-count: 代表最大并发线程数

    ===================================
    xml文件这种方式不能指定线程池，只有方法想才可以指定线程池
    -->

    <test name="demo1">
        <classes>
            <class name="com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>

    <test name="demo2">
        <classes >
            <class name="com.course.testng.multiThread.MultiThreadOnXml2"/>
        </classes>

    </test>

</suite>